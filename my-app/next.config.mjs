/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;

module.exports = {
    reactStrictMode: true,
    images: {
      domains: ['your-domain.com'], // Add any domains you need for images
    },
  };    